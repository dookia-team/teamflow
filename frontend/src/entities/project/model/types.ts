export interface Project {
  no: number
  name: string
  slug: string
  role: string
  memberCount: number
}

export interface ProjectSummary {
  no: number
  name: string
  slug: string
  memberCount: number
}
